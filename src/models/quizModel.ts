
// Use JavaScript objects for in -memory storage.This can later be replaced by a database like MongoDB or PostgreSQL if needed.
export class QuizModel {

    // In-memory storage.
    public static quizzes: any[] = [];

    // In -memory storage.
    public static results: any[] = [];

}
